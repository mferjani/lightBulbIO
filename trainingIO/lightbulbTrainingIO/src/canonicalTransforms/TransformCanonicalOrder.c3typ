type TransformCanonicalOrder mixes Order transforms CanonicalOrder {
  quantity:               ~ expression "quantity"
  timestamp:              ~ expression "orderDate"
  promisedDeliveryDate:   ~ expression "deliveryDate"
  parent:                 ~ expression "{id: warehouse}"
  id:                     ~ expression "orderId"
}
